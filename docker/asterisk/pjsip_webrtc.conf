; === pjsip_webrtc.conf — generated by intercom-server ===
; DO NOT EDIT MANUALLY

; === webrtc: 200001 ===
[200001]
type=endpoint
transport=transport-ws
aors=200001
auth=200001
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200001]
type=auth
auth_type=userpass
username=200001
password=k0xG0YMzirHN2nhq50epPA

[200001]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200001 ===




; === webrtc: 200002 ===
[200002]
type=endpoint
transport=transport-ws
aors=200002
auth=200002
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200002]
type=auth
auth_type=userpass
username=200002
password=iRO3hXlrIUhO4l6Zyw7Aww

[200002]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200002 ===




; === webrtc: 200003 ===
[200003]
type=endpoint
transport=transport-ws
aors=200003
auth=200003
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200003]
type=auth
auth_type=userpass
username=200003
password=HvWxUcN4_eCVsrh3GfJ7Cw

[200003]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200003 ===

; === webrtc: 200004 ===
[200004]
type=endpoint
transport=transport-ws
aors=200004
auth=200004
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200004]
type=auth
auth_type=userpass
username=200004
password=euWz4eRg9fQgAviRIPwT8g

[200004]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200004 ===




; === webrtc: 200010 ===
[200010]
type=endpoint
transport=transport-ws
aors=200010
auth=200010
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200010]
type=auth
auth_type=userpass
username=200010
password=OXFyXFUP2SFOdp5W1v8VYw

[200010]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200010 ===




; === webrtc: 200011 ===
[200011]
type=endpoint
transport=transport-ws
aors=200011
auth=200011
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200011]
type=auth
auth_type=userpass
username=200011
password=HmGiA-3TutPfrcFztSFVlA

[200011]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200011 ===

; === webrtc: 200012 ===
[200012]
type=endpoint
transport=transport-ws
aors=200012
auth=200012
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200012]
type=auth
auth_type=userpass
username=200012
password=PG28Ht0uB7Wx48oCQIOl8A

[200012]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200012 ===

; === webrtc: 200019 ===
[200019]
type=endpoint
transport=transport-ws
aors=200019
auth=200019
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200019]
type=auth
auth_type=userpass
username=200019
password=Sm4AZWvA8MgfffirqHwLtg

[200019]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200019 ===

; === webrtc: 200022 ===
[200022]
type=endpoint
transport=transport-ws
aors=200022
auth=200022
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200022]
type=auth
auth_type=userpass
username=200022
password=GfvJInWLRIDuYVs3Gm1bCA

[200022]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200022 ===

; === webrtc: 200026 ===
[200026]
type=endpoint
transport=transport-ws
aors=200026
auth=200026
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200026]
type=auth
auth_type=userpass
username=200026
password=fCRyzKz4vtsIin7veDz6dw

[200026]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200026 ===

; === webrtc: 200027 ===
[200027]
type=endpoint
transport=transport-ws
aors=200027
auth=200027
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200027]
type=auth
auth_type=userpass
username=200027
password=Nwdi2ji2MmtORmFRhy2RnA

[200027]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200027 ===

; === webrtc: 200028 ===
[200028]
type=endpoint
transport=transport-ws
aors=200028
auth=200028
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200028]
type=auth
auth_type=userpass
username=200028
password=xnxHRqWU4II0O6wd1TULQg

[200028]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200028 ===

; === webrtc: 200029 ===
[200029]
type=endpoint
transport=transport-ws
aors=200029
auth=200029
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200029]
type=auth
auth_type=userpass
username=200029
password=QenRX1jL_pElZrWlV2XblA

[200029]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200029 ===

; === webrtc: 200030 ===
[200030]
type=endpoint
transport=transport-ws
aors=200030
auth=200030
context=from-internal
disallow=all
allow=opus,ulaw,alaw
; webrtc=yes implies: rtcp_mux + use_avpf + ice_support + media_use_received_transport
webrtc=yes
use_avpf=yes
media_encryption=dtls
dtls_verify=fingerprint
dtls_setup=actpass
ice_support=yes
media_use_received_transport=yes
rtcp_mux=yes
; --- WebRTC over NAT must NOT use these Asterisk defaults: ---
; direct_media=true would force peer-to-peer RTP between the panel (plain UDP)
; and the WebRTC client (DTLS-SRTP) — they cannot talk directly. Asterisk has
; to stay in the media path and bridge SRTP↔RTP.
direct_media=no
; rewrite_contact=true makes Asterisk replace the Contact URI (which a SIP.js
; client sends as sip:foo@*.invalid:0) with the real socket address. Without
; this re-INVITEs and Hangups can't reach the WS contact.
rewrite_contact=yes
; rtp_symmetric=true sends RTP back to the source the packets came from rather
; than to the SDP-advertised address (which behind NAT is wrong).
rtp_symmetric=yes
force_rport=yes

[200030]
type=auth
auth_type=userpass
username=200030
password=dcXV8X1pPOFiIG0Q9NBJCQ

[200030]
type=aor
max_contacts=5
; remove_existing=no: on a reconnect race we want the OLD contact to survive
; long enough that an incoming Dial can still ring SOMETHING — otherwise the
; INVITE goes into the void during Doze-induced re-register (~10s window).
remove_existing=no
; default_expiration=120: extends contact lifetime so a single 60-90s mobile
; sleep (Doze, Wi-Fi/LTE switch) doesn't immediately invalidate the AOR.
default_expiration=120
; qualify_frequency=0: do NOT poll the WS contact with OPTIONS. Mobile in
; Doze can't answer those — and a missed OPTIONS marks the contact
; Unreachable, defeating the longer expiration above.
qualify_frequency=0
; === end webrtc: 200030 ===
