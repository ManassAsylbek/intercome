; === extensions_apartments.conf — generated by intercom-server ===
; DO NOT EDIT MANUALLY

[intercom-apartments]

; === apt: 10 ===
exten => 10,1,NoOp(Call to apartment 10)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=10)
 same => n,Dial(PJSIP/10,30,tT)
 same => n,Hangup()
; === end apt: 10 ===

; === apt: 100 ===
exten => 100,1,NoOp(Call to apartment 100)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=100)
 same => n,Dial(PJSIP/100,30,tT)
 same => n,Hangup()
; === end apt: 100 ===

; === apt: 100001 ===
exten => 100001,1,NoOp(Call to apartment 100001)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=100001)
 same => n,Dial(PJSIP/100001,30,tT)
 same => n,Hangup()
; === end apt: 100001 ===

; === apt: 10009 ===
exten => 10009,1,NoOp(Call to apartment 10009)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=10009)
 same => n,Dial(PJSIP/10009,30,tT)
 same => n,Hangup()
; === end apt: 10009 ===

; === apt: 1003 ===
exten => 1003,1,NoOp(Call to apartment 1003)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=1003)
 same => n,Dial(PJSIP/1003&PJSIP/200001,30,tT)
 same => n,Hangup()
; === end apt: 1003 ===

; === apt: 1004 ===
exten => 1004,1,NoOp(Call to apartment 1004)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=1004)
 same => n,Dial(PJSIP/1004&PJSIP/200002,30,tT)
 same => n,Hangup()
; === end apt: 1004 ===

; === apt: 1006 ===
exten => 1006,1,NoOp(Call to apartment 1006)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=1006)
 same => n,Dial(PJSIP/1006&PJSIP/200022,30,tT)
 same => n,Hangup()
; === end apt: 1006 ===

; === apt: 1008 ===
exten => 1008,1,NoOp(Call to apartment 1008)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=1008)
 same => n,Dial(PJSIP/1008,30,tT)
 same => n,Hangup()
; === end apt: 1008 ===

; === apt: 1010 ===
exten => 1010,1,NoOp(Call to apartment 1010)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=1010)
 same => n,Dial(PJSIP/1010,30,tT)
 same => n,Hangup()
; === end apt: 1010 ===

; === apt: 1043 ===
exten => 1043,1,NoOp(Call to apartment 1043)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=1043)
 same => n,Dial(PJSIP/1043,30,tT)
 same => n,Hangup()
; === end apt: 1043 ===

; === apt: 111 ===
exten => 111,1,NoOp(Call to apartment 111)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=111)
 same => n,Dial(PJSIP/111,30,tT)
 same => n,Hangup()
; === end apt: 111 ===

; === apt: 1111 ===
exten => 1111,1,NoOp(Call to apartment 1111)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=1111)
 same => n,Dial(PJSIP/1111,30,tT)
 same => n,Hangup()
; === end apt: 1111 ===

; === apt: 1222 ===
exten => 1222,1,NoOp(Call to apartment 1222)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=1222)
 same => n,Dial(PJSIP/1222,30,tT)
 same => n,Hangup()
; === end apt: 1222 ===

; === apt: 301 ===
exten => 301,1,NoOp(Call to apartment 301)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=301)
 same => n,Dial(PJSIP/200026&PJSIP/301,30,tT)
 same => n,Hangup()
; === end apt: 301 ===

; === apt: 302 ===
exten => 302,1,NoOp(Call to apartment 302)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=302)
 same => n,Dial(PJSIP/200027&PJSIP/302,30,tT)
 same => n,Hangup()
; === end apt: 302 ===

; === apt: 303 ===
exten => 303,1,NoOp(Call to apartment 303)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=303)
 same => n,Dial(PJSIP/200028&PJSIP/303,30,tT)
 same => n,Hangup()
; === end apt: 303 ===

; === apt: 304 ===
exten => 304,1,NoOp(Call to apartment 304)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=304)
 same => n,Dial(PJSIP/200029&PJSIP/304,30,tT)
 same => n,Hangup()
; === end apt: 304 ===

; === apt: 305 ===
exten => 305,1,NoOp(Call to apartment 305)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=305)
 same => n,Dial(PJSIP/200030&PJSIP/305,30,tT)
 same => n,Hangup()
; === end apt: 305 ===

; === apt: 8888 ===
exten => 8888,1,NoOp(Call to apartment 8888)
 ; __CALL_ID / __APARTMENT_CODE are inherited channel variables — they let
 ; the cloud re_invite_apartment handler find this exact panel channel later
 ; (e.g. after a mobile Doze wake-up) via AMI Status / Getvar.
 same => n,Set(__CALL_ID=${UNIQUEID})
 same => n,Set(__APARTMENT_CODE=8888)
 same => n,Dial(PJSIP/8888,30,tT)
 same => n,Hangup()
; === end apt: 8888 ===

